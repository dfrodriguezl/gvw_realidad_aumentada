// Componente para la consulta y despliegue de la información de los recuentos sincronizados por la aplicación móvil RUE

import React,{useState} from 'react';
import {variables} from '../base/variables';

// let layers = variables.layers;

const RUE=({})=> { 
    console.log("Entre a RUE")

    return "Entre a RUE"
}

export default RUE;